# API Base URL
# Development: Use localhost with port 5001 (macOS AirPlay uses port 5000)
# Production: Will be set via environment variable or use relative path with IIS proxy
VITE_API_BASE_URL=http://localhost:5001
